import 'package:flutter/material.dart';

///  This View Class would display some(12) routine elements chronologically

class NextUp extends StatelessWidget {
  const NextUp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
